1. Создает класс `ShelterBot`, наследующийся от `TelegramLongPollingBot`.
2. Хранит данные пользователя и информацию о приютах в двух `HashMap` объектах.
3. В конструкторе `ShelterBot` инициализирует информацию о приютах.
4. Обрабатывает входящие сообщения в методе `onUpdateReceived`.
5. Обрабатывает команды и отображает соответствующие меню и информацию о приютах.
6. Определяет методы `getBotUsername` и `getBotToken` для получения имени бота и токена.

